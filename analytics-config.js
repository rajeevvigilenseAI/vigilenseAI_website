// Google Tag Manager container ID (e.g. GTM-ABC1234 from tagmanager.google.com).
// When set, GTM loads site-wide and you should add the Leadsourcing pixel as a
// Custom HTML tag in GTM (All Pages trigger). Leave empty for direct pixel mode.
window.VIGILENSE_GTM_ID = '';
